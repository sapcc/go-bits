# Configuration file for <https://github.com/sapcc/go-makefile-maker>

githubWorkflow:
  ci:
    enabled: true
    coveralls: true
    ignorePaths: [ "**.md" ] # all Markdown files
  license:
    enabled: true
  securityChecks:
    enabled: true
  spellCheck:
    enabled: true

golangciLint:
  createConfig: true

renovate:
  enabled: true
  assignees:
    - majewsky
    - SuperSandro2000
